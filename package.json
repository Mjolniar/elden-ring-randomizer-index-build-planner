{
  "name": "elden-ring-index",
  "version": "1.2.0-preview.2",
  "description": "Searchable index and build planner for Elden Ring — all items, vanilla locations, stat-filtered builds.",
  "author": "Mjolniar",
  "private": true,
  "main": "electron/main.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "electron:dev": "concurrently -k -n vite,electron -c cyan,yellow \"vite\" \"wait-on http://localhost:5173 && electron .\"",
    "dist": "npm run build && electron-builder --win --x64 && npx tsx scripts/clean-release.ts"
  },
  "dependencies": {
    "jsdom": "^29.1.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "concurrently": "^8.2.2",
    "electron": "^31.0.0",
    "electron-builder": "^24.13.3",
    "typescript": "^5.5.3",
    "vite": "^5.3.4",
    "vitest": "^2.0.5",
    "wait-on": "^7.2.0"
  },
  "build": {
    "appId": "com.mjolniar.eldenring.index",
    "productName": "Elden Ring Index and Build Planner",
    "copyright": "MIT",
    "asar": true,
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**",
      "electron/main.js",
      "electron/preload.js",
      "package.json"
    ],
    "artifactName": "${productName} ${version} Portable.${ext}",
    "win": {
      "signAndEditExecutable": false,
      "icon": "assets/icon-vanilla.png",
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        },
        {
          "target": "zip",
          "arch": ["x64"]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "shortcutName": "Elden Ring Index and Build Planner",
      "artifactName": "${productName} ${version} Setup.${ext}",
      "runAfterFinish": false,
      "deleteAppDataOnUninstall": false
    }
  }
}
